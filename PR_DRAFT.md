## 💡 PR 제목
> 예: feat: [JHW] Redis 직렬화 설정 및 NCP 키 환경변수화

---

## 🔗 관련 이슈
> 진행 중이면 `Refs #123`, 완전 해결이면 `Closes #123` (완전히 끝났을 때만)
- Refs/Closes: #

---

## 📌 작업 내용 요약
- [x] Redis 캐시 직렬화에 `JavaTimeModule` 등록 (LocalDateTime 직렬화 오류 방지)
- [x] NCP Object Storage 키 하드코딩 제거 및 환경변수 전환
- [x] 테스트용 ReservationService 생성자 의존성 보강
- [ ] Redis 실제 연동 상태 점검 및 재시도 (실행 환경 확인 필요)
- [ ] 이미지 리사이징/업로드 파이프라인 실행 및 검증

---

## 🧱 변경 범위 (Scope)
### Backend
- [x] API 추가/수정
- [x] Validation/예외처리
- [ ] 인증/인가
- [ ] DB 스키마/쿼리
- [x] 기타: Redis 캐시 직렬화 설정, 테스트 의존성 보강, 스크립트 보안 처리

### Frontend
- [ ] UI/라우팅
- [ ] 상태관리/비동기 로직
- [ ] 입력값/검증
- [ ] 기타:

---

## 🧪 테스트 내역
### Backend
- [x] 로컬에서 애플리케이션 실행 확인
- [ ] 단위 테스트 통과
- [x] API 수동 테스트 (Postman / Swagger 등)

### Frontend
- [ ] `npm run dev` 로컬 실행 확인
- [ ] 주요 화면/기능 수동 테스트

### 테스트 상세(필수)
> 테스트한 API/페이지/케이스를 간단히 적어주세요.
- 예) 쿠폰 다운로드 가능 목록 API 호출 시 Redis 캐시 직렬화 오류 재현 여부 확인

---

## 🖼️ 화면 캡처 / 시연 영상 (Frontend 변경 시 권장)
- before:
- after:

---

## ⚠️ 영향도 / 리스크 / 롤백
> 리뷰어가 “머지해도 되는지” 판단하기 위한 섹션입니다.
- 영향도: Redis 캐시 직렬화 방식 변경 (LocalDateTime JSON 직렬화 활성화)
- 리스크: 캐시 직렬화 포맷 변경으로 기존 캐시 데이터와의 호환성 문제 가능
- 롤백 방법: 이전 `GenericJackson2JsonRedisSerializer` 기본 설정으로 되돌리기

---

## ✅ 리뷰어 체크 포인트 (선택)
> 리뷰어가 집중해서 봐줬으면 하는 부분이 있으면 적어주세요.
- Redis 캐시 직렬화 설정이 다른 캐시/도메인에 영향 없는지
- NCP 업로드 스크립트 환경변수 사용 방식 적절성

---

## ✨ 제안 내용 요약
> 어떤 기능/개선을 하고 싶은지 한 줄로 요약해주세요.
- Redis 캐시 직렬화 오류 제거 및 NCP 키 보안 처리

---

## 🎯 목표 / 완료 조건 (Acceptance Criteria)
> "이 이슈를 닫아도 된다"라고 합의 가능한 조건을 2~5개만 적어주세요.
> 가능하면 결과 중심(사용자 관점)으로 작성합니다.
- [ ] 쿠폰 목록 API 호출 시 Redis 캐시 직렬화 오류가 발생하지 않는다
- [ ] NCP 업로드 스크립트가 환경변수로 정상 동작한다
- [ ] Redis 연동 및 이미지 리사이징 파이프라인 검증이 완료된다

---

## 📌 상세 설명
> 어떤 동작을 기대하는지, 왜 필요한지 설명해주세요.

### 📂 도메인
> 개인 담당 도메인(파트) 표시 후 작성해주세요.
> ex) Dashboard / Host / Booking ...

- 현재 상황:
  - Redis 캐시 저장 시 LocalDateTime 직렬화 오류 발생
  - NCP 키가 스크립트에 하드코딩되어 푸시 차단 발생
  - Redis 실제 연동/이미지 리사이징 파이프라인은 아직 미검증
- 개선 내용:
  - Redis 캐시 직렬화 ObjectMapper에 JavaTimeModule 등록
  - NCP 키를 환경변수로 전환하고 `.env.example` 제공
- 기대 효과:
  - 캐시 저장 안정화, 키 노출 방지, 배포 보안 강화

---

## 🧱 작업 범위 (Scope)
> 대략 어떤 작업이 포함될지 체크해주세요.

### Backend
- [ ] API 추가
- [x] 기존 API 수정
- [ ] DB 스키마 변경
- [x] 테스트 코드(선택)
- [x] 기타

### Frontend
- [ ] 화면(UI) 추가
- [ ] 기존 화면 수정
- [ ] 상태 관리 로직 수정
- [ ] 기타

---

## 🔗 관련 이슈 / 화면 (선택)
- 관련 이슈: #
- 관련 화면: -
- 참고 링크: -

---

## ✅ 체크리스트
- [ ] 팀원과 간단히 논의 완료
- [ ] 우선순위(긴급/보통/나중에) 이야기됨
- [ ] 디자인/기획 이슈가 필요한 경우 정리됨
- [ ] 완료 조건(AC)을 2~5개 작성함

---

## 🧾 진행 로그 (작업 중/마감 시 댓글로 남기기)
> 작업 종료 시 아래 형식으로 **댓글 1회** 남겨주세요.
- 오늘 진행: Redis 캐시 직렬화 오류 수정, NCP 키 환경변수화, 테스트 의존성 보강
- 남은 작업: Redis 실연동 확인, 이미지 리사이징/업로드 파이프라인 검증
- Blocker(있으면): 로컬 환경/인프라 준비 상태 확인 필요
- 다음 액션(내일 할 것): Redis/이미지 파이프라인 재시도 및 테스트

---

## 🔀 PR 연결 규칙 (중요)
> PR 본문에 아래 중 하나로 이슈를 연결해주세요.
- 진행 중(일부만 반영): `Refs #이슈번호`
- 완료(이슈 닫아도 됨): `Closes #이슈번호` ✅ **완전히 끝났을 때만**
